{
  "name": "shamela-mcpb",
  "version": "1.2.0",
  "description": "MCPB (MCP Bundle) for searching and studying a local Maktabah al-Shamela 4 install from Claude Desktop. 30 read-only tools.",
  "author": {
    "name": "h.alhoqbani",
    "email": "h.alhoqbani@gmail.com",
    "url": "https://github.com/alhoqbani"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alhoqbani/shamela-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/alhoqbani/shamela-mcp/issues"
  },
  "homepage": "https://github.com/alhoqbani/shamela-mcp#readme",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:server": "esbuild src/server/index.ts --bundle --platform=node --target=node20 --format=esm --loader:.wasm=binary --outfile=dist/index.js --banner:js=\"import { createRequire as __cr } from 'module'; import { fileURLToPath as __furl } from 'url'; import { dirname as __dn } from 'path'; const require = __cr(import.meta.url); const __filename = __furl(import.meta.url); const __dirname = __dn(__filename);\"",
    "build:java": "node scripts/build-java.mjs",
    "build": "npm run build:server && npm run build:java",
    "smoke": "tsx tests/smoke.ts",
    "benchmark": "tsx tests/benchmark.ts",
    "pack": "node scripts/pack.mjs",
    "release": "node scripts/release.mjs",
    "release:dry": "node scripts/release.mjs --dry-run",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "sql.js": "^1.14.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "@types/sql.js": "^1.4.11",
    "@vitest/coverage-v8": "^4.1.5",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.5"
  }
}
